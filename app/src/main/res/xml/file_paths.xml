<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- 缓存目录，用于相机拍照临时存储 -->
    <cache-path
        name="cache"
        path="." />

    <!-- 外部缓存目录 -->
    <external-cache-path
        name="external_cache"
        path="." />

    <!-- 外部文件目录 -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- 图片目录 -->
    <external-files-path
        name="images"
        path="Pictures/" />
</paths>
